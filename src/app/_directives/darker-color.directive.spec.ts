import { DarkerColorDirective } from './darker-color.directive';

describe('DarkerColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DarkerColorDirective();
    expect(directive).toBeTruthy();
  });
});
