import { HeaderHeightDirective } from './header-height.directive';

describe('HeaderHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderHeightDirective();
    expect(directive).toBeTruthy();
  });
});
