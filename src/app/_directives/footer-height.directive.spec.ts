import { FooterHeightDirective } from './footer-height.directive';

describe('FooterHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new FooterHeightDirective();
    expect(directive).toBeTruthy();
  });
});
